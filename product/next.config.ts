import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "theme.hstatic.net",
      "product.hstatic.net",
      "www.laneige.com",
      "www.etude.com",
      "www.sulwhasoo.com",
    ],
  },
};

export default nextConfig;
