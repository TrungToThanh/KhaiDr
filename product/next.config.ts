import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "theme.hstatic.net",
      "product.hstatic.net",
    ],
  },
};

export default nextConfig;
